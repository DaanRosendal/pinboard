{
  "name": "acme-platform",
  "version": "0.0.0",
  "private": true,
  "workspaces": ["apps/*", "services/*", "packages/*", "tools/*", "e2e"],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "prettier": "^3.2.0"
  },
  "packageManager": "pnpm@9.0.0"
}
