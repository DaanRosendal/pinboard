# Contributing

## Setup

```bash
pnpm install
pnpm build
```

## Workflow

1. Create a feature branch from `main`
2. Run `pnpm test` and `pnpm lint` before pushing
3. Open a PR against `main`

## Commit convention

This project follows [Conventional Commits](https://www.conventionalcommits.org/).
